{
	"name": "webhook-capture",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"types": "wrangler types",
		"check": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"migrate:local": "wrangler d1 migrations apply webhook-capture --local"
	},
	"dependencies": {
		"hono": "^4.9.8"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^24.5.2",
		"typescript": "^5.9.2",
		"vitest": "^3.2.4",
		"wrangler": "^4.85.0"
	}
}
